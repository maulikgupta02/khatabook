# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

Pinned to SDK 56 (not the latest 57) because Expo Go on SDK 57 crashes natively (SIGSEGV in libhermesvm.so/libworklets.so) on startup on this Android emulator — reproducible even after a clean Expo Go data wipe. Don't bump back to 57 without confirming that's fixed upstream.
